version: 2

project_name: progressa

dist: .builds

before:
  hooks:
    - npm --prefix=./ui ci
    - npm --prefix=./ui run build
    - go mod tidy

builds:
  - id: progressa_build
    main: ./main.go
    binary: progressa
    ldflags:
      - -s -w -X main.Version={{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

release:
  draft: true

archives:
  - id: progressa_archive
    ids: [progressa_build]
    formats: zip
    files:
      - src: dist/pb_migrations/*
        dst: pb_migrations/
      - LICENSE.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
